<%= form_with(model: lead_magnet) do |form| %>
  <% if lead_magnet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(lead_magnet.errors.count, "error") %> prohibited this lead_magnet from being saved:</h2>

      <ul>
        <% lead_magnet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div>
    <%= form.label :ai_template, style: "display: block" %>
    <%= form.text_field :ai_template, class: "form-control"  %>
  </div>

  <div>
    <%= form.label :prompt, style: "display: block" %>
    <%= form.text_area :prompt, class: "form-control"  %>
  </div>

  <div>
    <%= form.submit "Create", class: "btn btn-sm btn-primary" %>
  </div>
<% end %>
